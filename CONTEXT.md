# STM32G474 SimpleFOC 扭矩控制 Demo

基于 NUCLEO-G474RE 与 DRV8316CT 驱动板的 BLDC 电机扭矩控制演示项目。核心目标：用 SimpleFOC 实现 20kHz 电流环（=扭矩控制），通过串口接收电流指令。

## Language

**扭矩控制**：
本项目的唯一控制模式。SimpleFOC 中 `torque_controller = current`：电流环直接控制转子正交电流，指令单位是安培。不含速度环与位置环。
_Avoid_: 电流模式、开环扭矩

**目标电流**：
通过串口（Commander）下发的扭矩指令，单位 A。实际扭矩 = 目标电流 × 扭矩常数。
_Avoid_: target、扭矩指令（避免与扭矩 N·m 单位混淆时使用）

**扭矩常数（Kt）**：
本电机为 0.23 N·m/A。电流 → 扭矩的换算系数。
_Avoid_: 扭矩系数

**反电动势常数（Ke）**：
本电机为 0.16 V/(rad/s)（线-线反电动势）。

**极对数**：
本电机为 11（24N22P，Delta 绕组）。电角 = 机械角 × 11。

**IPROPI**：
DRV8316CT 的内置电流检测。每相一个模拟输出（SOA/SOB/SOC，即 IPROPI1/2/3），经板上引脚配置固定增益（无 SPI 可配置，增益取值需现场标定）。区别于外置采样电阻方案，SimpleFOC 无现成支持。
_Avoid_: 电流采样电阻

**3PWM 模式**：
DRV8316CT 的输入模式：INHx 接主控 PWM，INLx 接地，由驱动板内部生成互补驱动。对应 SimpleFOC 的 `BLDCDriver3PWM`。

**ABZ 编码器**：
MT6701 磁编码器的输出模式：A/B 正交脉冲对（本项目 1024 线/圈）+ 每圈一个 Z 索引脉冲。Z 用于上电角度对齐。采用 OTP 出厂默认配置，不做 SPI 动态配置。
_Avoid_: 磁编、SPI 模式

**电压对齐（voltage aligning）**：
上电校准流程：对电机施加固定电压矢量使转子锁到已知电角，从而标定编码器方向与电角零位。
_Avoid_: 对齐校准（无歧义时简写"对齐"）

**母线电压**：
本项目供电 24V（12–36V 范围）。SimpleFOC 的电压上限与占空比计算基准。

## 关系

- 扭矩控制 = 电流环 = 正交电流控制；目标电流经扭矩常数换算为扭矩
- IPROPI 三路输出 → 主控 ADC1 → 自定义 CurrentSense → SimpleFOC 电流环反馈
- ABZ 编码器（机械角 × 极对数 = 电角）→ 电流环坐标变换的角度来源
